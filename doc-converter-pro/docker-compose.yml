version: '3.8'
services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      - PORT=3000
      - DOC_CONVERTER_MAX_FILE_BYTES=10737418240
    volumes:
      - ./tmp:/app/tmp
