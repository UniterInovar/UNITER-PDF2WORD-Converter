# Brand Verification

The rendered homepage header now displays **UNITER document converter** with uppercase `UNITER` and lowercase `document converter`, matching the final approved casing. The browser title and description use the same brand, and the managed `VITE_APP_TITLE` value has been updated accordingly.
